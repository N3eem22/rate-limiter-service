# rate-limiter-service
A distributed rate limiter service built from scratch using .NET, inspired by system design principles. Supports token bucket, sliding window, and fixed window algorithms. Packaged as a Dockerized microservice for use across multiple projects.
